# src/rne_cli/commands/history.py (stub — real impl in Task 18)
import typer


def history_cmd(ctx: typer.Context, siren: str = typer.Argument(...)):
    """[stub]"""
    typer.echo(f"stub historique {siren}")
